# paths-to-remove.txt — committed, PII-free path-removal list for git-filter-repo.
#
# Consumed by scripts/publish/gmj_publish_mirror.sh via:
#   git filter-repo --force --invert-paths --paths-from-file <(this file)
#
# One path or glob per line. filter-repo path-globs are LITERAL (fnmatch-style),
# matched against the path as recorded in each commit, across ALL history.
#
# KEEP fixtures (deliberately NOT matched by any glob below — verified by listing
# them explicitly here, not via an exclude directive):
#   tests/fixtures/offers/sample-offer.txt
#   tests/fixtures/offers/sample-offer-draft.json
#   sources/README.md
#   sources/candidate/README.md
#   .gitkeep files (e.g. output/cv/.gitkeep, output/analysis/.gitkeep, ...)
#
# No real PII value appears in this file — only paths and globs.

# Private planning workspace — never mirrored publicly.
.planning/

# Real per-offer generated artifact drafts (CV/cover-letter/interview-prep JSON).
# NOTE: output/artifacts/* is git-ignored (Plan 42-01) for FUTURE additions only —
# it does NOT retroactively untrack the pre-existing softpeak-lead-backend-php-engineer-laravel
# draft files that were already committed (via git mv from sources/artifacts/) before that
# ignore rule existed. `git check-ignore` confirms those files are NOT ignored. This glob is
# the actual (sole) protection for those already-tracked files, not defense-in-depth.
output/artifacts/

# Real generated interview-prep markdown docs.
glob:output/interview-prep-*.md

# Real personal source documents (diplomas, certificates, CVs, badges, photo).
glob:sources/*.pdf
glob:sources/**/*.pdf
sources/user_photo.jpg
glob:sources/*.jpg

# Real PII map + denylist must never reach the public repo, even though they are
# gitignored in the private repo (defense in depth against an accidental commit).
scripts/publish/replacements.txt
scripts/publish/pii-denylist.txt

# This repo's own self-publishing CI workflow — meaningless in the destination
# repo (it pushes back to give-me-job-now via a PAT scoped to THIS operation),
# and shipping it would require the publish PAT to carry the broader `workflow`
# scope just to push a file that does nothing there.
.github/workflows/publish-mirror.yml

# public-assets/ is the SOURCE for the injected README.md + LICENSE (read from
# $REPO_ROOT, not the temp clone — see gmj_publish_mirror.sh's "Inject public
# docs" step), so removing the directory itself from mirrored history does not
# break that injection; it just avoids shipping a redundant copy.
public-assets/

# Internal TUI dashboard design/testing notes — not part of the public project.
TUI/

# Internal presentation prep material (prompts, speaker notes, review drafts)
# — not part of the public project.
Presentation/

# Internal gitleaks scan config (this repo's own manifest-hash false-positive
# allowlist) — not relevant to the public repo. Safe to strip: the publisher's
# gitleaks step now reads .gitleaks.toml from $REPO_ROOT directly (same
# pattern as public-assets/'s README/LICENSE injection), not from this mirror.
.gitleaks.toml

# This file contains unredacted real PII (a phone number and personal email
# addresses) not caught by the --replace-text redaction pass in
# gmj_publish_mirror.sh — an active leak in the current public mirror.
# git-filter-repo path lines are literal and match the path byte-for-byte,
# including this filename's embedded space, so no quoting/escaping is needed.
sources/CV compilation.txt

# The web/ folder has no functional CI/deploy role in the public mirror: the
# live Vercel deploy runs from this private repo's feature/gsd branch, and
# gmj_publish_mirror.sh only ever pushes feature/gsd to the public remote's
# main branch — feature/gsd itself never exists in the mirror's history, so
# nothing in web/ can execute there.
web/

# GitHub Pages support is being dropped as a public deploy path, so these
# three workflow files ship no working CI to the mirror: build-site.yml is
# scoped to web/** (dead once web/ above is excluded), publish-site.yml is
# already documented elsewhere as dormant/broken, and
# publish-site-vercel.yml's push trigger targets feature/gsd — a branch
# absent from the mirror, so it can never fire there either.
.github/workflows/build-site.yml
.github/workflows/publish-site.yml
.github/workflows/publish-site-vercel.yml
